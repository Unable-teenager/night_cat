{
	"name": "getPosts",
	"version": "1.0.0",
	"description": "Cloud function to fetch community posts.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"wx-server-sdk": "^2.6.0"
	}
}
